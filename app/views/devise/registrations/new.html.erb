<section class="vh-100 gradient-custom">
  <div class="container py-5 h-100">
    <div class="row justify-content-center align-items-center h-100">
      <div class="col-12 col-lg-9 col-xl-7">
        <div class="card bg-black bg-opacity-75 text-white p-5 text-center" style="border-radius: 15px;">
          <div class="card-body p-4 p-md-5">
            <h3 class="mb-4 pb-2 pb-md-0 mb-md-5">Registration Form</h3>

            <!-- Your registration form HTML goes here -->

            <form>
              <!-- ... (rest of the registration form) ... -->
            </form>

            <hr class="my-5">

            <h2>Sign up</h2>

            <!-- Devise Sign up form -->
            <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
              <%= render "devise/shared/error_messages", resource: resource %>

              <div class="field">
  <%= f.label :first_name %><br />
  <%= text_field_tag :first_name, nil, class: "form-control form-control-lg" %>
</div>

<div class="field">
  <%= f.label :last_name %><br />
  <%= text_field_tag :last_name, nil, class: "form-control form-control-lg" %>
</div>

          

              <div class="field">
                <%= f.label :email %><br />
                <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control form-control-lg" %>
              </div>

              <div class="field">
                <%= f.label :password %>
                <% if @minimum_password_length %>
                <em>(<%= @minimum_password_length %> characters minimum)</em>
                <% end %><br />
                <%= f.password_field :password, autocomplete: "new-password", class: "form-control form-control-lg" %>
              </div>

              <div class="field">
                <%= f.label :password_confirmation %><br />
                <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control form-control-lg" %>
              </div>

              <div class="actions mt-4">
                <%= f.submit "Sign up", class: "btn btn-primary btn-lg", style: "background-color: #E4405F; color: #FFFFFF; border-color: #FF1493;" %>
              </div>

            <% end %>

            <%= render "devise/shared/links" %>

          </div>
        </div>
      </div>
    </div>
  </div>
</section>